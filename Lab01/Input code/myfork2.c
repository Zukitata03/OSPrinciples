#include <stdio.h>#include <unistd.h>#include <stdlib.h>int main() {    int pid = fork();    if (pid < 0) {        perror("fork");        exit(-1);    }    if (pid == 0) {        printf("medium (PID: %d)\n", getpid());        fork();        fork();        fork();        printf("small (PID: %d)\n", getpid());    } else {        printf("big (PID: %d)\n", getpid());    }    return 0;}